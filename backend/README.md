# backend/

Backend de nível de plataforma (`api/`, `agents/`, `platform/`, `security/`, `telemetry/`), distinto do núcleo em `sentinel_platform/` e dos backends de cada produto.

**Status:** vazio de propósito. Existe um diretório `backend/` órfão na raiz de `sentinel-os`/`homolog` (confirmado código morto — nenhum import, Procfile ou pm2 aponta pra lá) que foi arquivado na branch `legacy/root-backend-orfao` em vez de migrado para cá, já que não representa nada em uso. Não migrar conteúdo para esta pasta sem antes confirmar um propósito real e ativo.
