# shared/

Componentes, tipos, utilitários e schemas compartilhados entre os frontends/backends de todos os produtos.

**Status:** esqueleto vazio — não existe em nenhum dos 3 repositórios hoje. Criação de conteúdo real é trabalho de desenvolvimento de produto, fora do escopo desta migração estrutural (Fase 7 só cria as subpastas `components/`, `types/`, `utilities/`, `schemas/` com este aviso).
