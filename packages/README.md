# packages/

Pacotes reutilizáveis publicáveis/versionáveis independentemente (ex: SDKs internos, bibliotecas compartilhadas).

**Status:** esqueleto vazio — não existe em nenhum dos 3 repositórios hoje. Sem conteúdo definido ainda; criar quando houver um caso de uso real de pacote reutilizável entre produtos.
