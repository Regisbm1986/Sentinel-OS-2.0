# Status: resume/, interview/

Nenhuma dessas subpastas existe hoje. A lógica de currículo está dispersa dentro de `app/backend/career/` e `app/backend/app/main.py` (não é um módulo nomeado `resume/`). `interview/` não tem nenhuma implementação em nenhum repositório — é funcionalidade ainda não construída, não uma migração pendente.
